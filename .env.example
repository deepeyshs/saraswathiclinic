# No variables are required for this site. All are optional.
# Copy to .env for local use. Never commit a real .env file.

# Build target: cloudflare-module (default), node-server, vercel, netlify
# NITRO_PRESET=node-server

# Node server port (node-server preset only)
# PORT=3000

# NODE_ENV=production
